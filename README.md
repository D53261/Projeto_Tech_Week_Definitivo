# README Inicial

### Lembrar de rodar o projeto usando docker-compose (tendo docker instalado na maquina), para isso basta usar o comando:

```bash
docker-compose up --build
```

### Preencher mais tarde com tudo pronto